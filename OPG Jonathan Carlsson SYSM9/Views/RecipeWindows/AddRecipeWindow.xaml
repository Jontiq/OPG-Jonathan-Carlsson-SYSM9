<Window x:Class="OPG_Jonathan_Carlsson_SYSM9.Views.RecipeWindows.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OPG_Jonathan_Carlsson_SYSM9.Views.RecipeWindows"
        mc:Ignorable="d"
        Title="AddRecipeWindow" Height="500" Width="510" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="1.2*"/>
        </Grid.RowDefinitions>
            <TextBlock Text="Register New Recipe"
               HorizontalAlignment="Center"
               VerticalAlignment="Top"
               FontSize="20"
               FontWeight="Bold"/>
            <!--TITLE-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2.5*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Title: "
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       FontSize="15"/>
            <TextBox Grid.Column="1"
                     Text="{Binding Title,UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     FontSize="15"
                     Margin="5"/>
        </Grid>
        <!--CATEGORIES-->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2.5*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Categories "
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   FontSize="15"/>
                <ComboBox Grid.Column="1"
                  ItemsSource="{Binding Categories}"
                  SelectedItem="{Binding SelectedCategory,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                  VerticalAlignment="Center"
                  FontSize="15"
                  Margin="5"/>
            </Grid>
        <!--INSTRUCTIONS-->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2.5*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Instructions: "
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       FontSize="15"/>
            <TextBox Grid.Column="1"
                     Text="{Binding Instructions,UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Height="100"
                     FontSize="15"
                     Margin="5"/>
        </Grid>
        <!--INGREDIENTS-->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2.5*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Ingredients:"
               HorizontalAlignment="Right"
               VerticalAlignment="Top"
               FontSize="15"
               Margin="5"/>

            <TextBox Grid.Column="1"
             Text="{Binding Ingredients, UpdateSourceTrigger=PropertyChanged}"
             AcceptsReturn="True"
             VerticalScrollBarVisibility="Auto"
             TextWrapping="Wrap"
             FontSize="15"
             Height="100"
             Margin="5"/>
        </Grid>

        <!--"GOBACK" AND "SAVE" BUTTON-->
            <Grid Grid.Row="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="Go Back"
                Command="{Binding GoBackCommand}"
                Margin="10"
                Width="150"/>
                <Button Content="Add New Recipe"
                Command="{Binding AddNewRecipeCommand}"
                Grid.Column="1"
                Margin="10"
                Width="150"/>
            </Grid>
        </Grid>
</Window>
