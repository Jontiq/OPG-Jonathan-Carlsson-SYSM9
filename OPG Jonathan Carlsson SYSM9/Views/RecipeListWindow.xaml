<Window x:Class="OPG_Jonathan_Carlsson_SYSM9.Views.RecipeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OPG_Jonathan_Carlsson_SYSM9.Views"
        mc:Ignorable="d"
        Title="RecipeListWindow" Height="450" Width="800"
        ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!--HEADLINE-->
            <TextBlock Text="Recipe List"
                       HorizontalAlignment="Center"
                       FontSize="35"
                       FontWeight="Bold"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <Rectangle Width="5"/>
                    <TextBlock VerticalAlignment="Center"
                           Text="Logged in as: "/>
                    <!--USERNAME PRESENTATION-->
                    <TextBlock VerticalAlignment="Center"
                           Text="{Binding LoggedInUsername}"/>
                    <TextBlock VerticalAlignment="Center"
                           Text=" ("/>
                    <!--ROLE PRESENTATION-->
                    <TextBlock VerticalAlignment="Center"
                           Text="{Binding LoggedInRole}"/>
                    <TextBlock VerticalAlignment="Center"
                           Text=")"/>
                    <TextBlock VerticalAlignment="Center"
                               Text=" ID: "/>
                    <!--ID PRESENTATION-->
                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding LoggedInID}"/>
                </StackPanel>
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal">
                    <Rectangle Width="23"/>
                    <TextBlock VerticalAlignment="Center"
                           Text="Filter: "/>
                    <!--FILTER INPUT-->
                    <TextBox Width="250"
                             Height="23"/>
                </StackPanel>
            </Grid>
        </Grid>
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Recipes}"
                  SelectedItem="{Binding SelectedRecipe, Mode=TwoWay}"
                  AutoGenerateColumns="False" 
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Recipe Title" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Date" Binding="{Binding Date,StringFormat='yyyy-MM-dd'}" Width="150" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>
        <!--GRID TO THE RIGHT-->
        <Grid Grid.Row="0"
              Grid.RowSpan="2"
              Grid.Column="1">
            <!--BUTTONS-->
            <StackPanel>
                <Button Content="Add Recipe"
                        Command="{Binding AddRecipeButtonCommand}"
                        Margin="5"/>
                <Button Content="Recipe Details"
                        Command="{Binding RecipeDetailsButton}"
                        Margin="5"/>
                <Button Content="Remove Recipe"
                        Margin="5"/>
                <Rectangle Height="80"/>
                <Button Content="Cookmaster Info"
                        Margin="5"/>
                <Rectangle Height="80"/>
                <Button Content="Edit User"
                        Margin="5"/>
                <Button Content="Sign Out"
                        Command="{Binding LogoutCommand}"
                        Margin="5"/>

            </StackPanel>
        </Grid>
    </Grid>
</Window>
